interface:
  display_name: "Generate Client Ad Video"
  short_description: "Lock five-script, Storyboard, and R2V production stages"
  default_prompt: "Use $generate-client-ad-video to build five script options, lock one Final Script, confirm one aggregate Storyboard, and create one authorized R2V H3 task."
dependencies:
  tools:
    - type: "mcp"
      value: "minimax_h3_console"
      description: "Upload the approved keyframe and submit or monitor MiniMax H3 video tasks"
policy:
  allow_implicit_invocation: true
