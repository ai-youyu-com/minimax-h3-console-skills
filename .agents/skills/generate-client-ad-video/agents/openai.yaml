interface:
  display_name: "Generate Client Ad Video"
  short_description: "快速生成五案、分镜与一个H3广告视频任务"
  default_prompt: "Use $generate-client-ad-video in Fast Mode to create five concise directions, one approved Storyboard, and one authorized H3 video task."
dependencies:
  tools:
    - type: "mcp"
      value: "minimax_h3_console"
      description: "Upload the approved keyframe and submit or monitor MiniMax H3 video tasks"
policy:
  allow_implicit_invocation: true
